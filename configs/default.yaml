model:
  hidden_dim: 64

training:
  batch_size: 16
  learning_rate: 0.001
  epochs: 12
  sequence_length: 12
  max_frames: null   # IMPORTANT: use FULL dataset

data:
  root_dir: "./data/UCSD_Anomaly_Dataset/UCSD_Anomaly_Dataset.v1p2"
  image_size: 128

logging:
  log_dir: "./runs/hyperparameter_tuning"
